/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'majjajfhoxucebhcbuih.supabase.co',
            },
        ],
    },
};

export default nextConfig;
